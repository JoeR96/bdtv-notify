So the issue with the, um, k8s cluster is that the HPA isn't scaling fast enough during the, like, morning traffic spike. The p99 latency is hitting, um, 2.3 seconds which is way over our SLO of 500ms. I think we need to lower the, you know, targetCPUUtilizationPercentage from 80 to like 60, and also maybe tune the scaleDown stabilizationWindowSeconds. Oh and also check whether the, um, VPA recommendations are sensible for the PostgreSQL pods because those are, like, memory-constrained right now.
